<Window x:Class="DeskManager.Windows.FenceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        MinWidth="130"
        MinHeight="38"
        Title="DeskManager"
        AllowDrop="True"
        Drop="Window_Drop"
        DragOver="Window_DragOver">

    <Window.Resources>
        <Style x:Key="HeaderBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#CCFFFFFF"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Width" Value="22"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#55FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="6"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#55FFFFFF" CornerRadius="3"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="4">
        <Border.Effect>
            <DropShadowEffect BlurRadius="8" Opacity="0.5" ShadowDepth="2" Color="#000000"/>
        </Border.Effect>

        <Border x:Name="MainBorder"
                CornerRadius="8"
                Background="{DynamicResource GridBackgroundBrush}"
                BorderBrush="{DynamicResource GridBorderBrush}"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition x:Name="ContentRow" Height="*"/>
                    <RowDefinition x:Name="ResizeRow" Height="8"/>
                </Grid.RowDefinitions>

                <!-- ═══ TITLE BAR ═══ -->
                <Border Grid.Row="0"
                        Background="{DynamicResource TitleBarBrush}"
                        CornerRadius="8,8,0,0"
                        Cursor="SizeAll"
                        MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
                        MouseRightButtonDown="TitleBar_MouseRightButtonDown">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="26"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="24"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0"
                                x:Name="CollapseBtn"
                                Style="{StaticResource HeaderBtn}"
                                Click="CollapseBtn_Click"
                                Margin="3,0,0,0">
                            <TextBlock x:Name="CollapseIcon" Text="▾" FontSize="11"/>
                        </Button>

                        <Grid Grid.Column="1">
                            <TextBlock x:Name="TitleLabel"
                                       Foreground="{DynamicResource GridTextBrush}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Margin="4,0"
                                       MaxWidth="160"
                                       Cursor="IBeam"
                                       TextTrimming="CharacterEllipsis"/>

                            <TextBox x:Name="TitleBox"
                                     Visibility="Collapsed"
                                     FontSize="12"
                                     VerticalAlignment="Center"
                                     Margin="4,2"
                                     Background="#33000000"
                                     Foreground="White"
                                     CaretBrush="White"
                                     BorderBrush="#55FFFFFF"
                                     BorderThickness="1"
                                     Padding="2,1"
                                     KeyDown="TitleBox_KeyDown"
                                     LostFocus="TitleBox_LostFocus"/>
                        </Grid>

                        <Button Grid.Column="2"
                                Style="{StaticResource HeaderBtn}"
                                Click="CloseBtn_Click"
                                Margin="0,0,3,0"
                                ToolTip="Remove fence">
                            <TextBlock Text="✕" FontSize="10"/>
                        </Button>
                    </Grid>
                </Border>

                <!-- ═══ CONTENT AREA ═══ -->
                <Grid x:Name="ContentArea" Grid.Row="1">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <WrapPanel x:Name="IconPanel"
                                   Orientation="Horizontal"
                                   Margin="6,4"
                                   AllowDrop="True"
                                   Drop="IconPanel_Drop"
                                   DragOver="IconPanel_DragOver"/>
                    </ScrollViewer>

                    <TextBlock x:Name="EmptyHint"
                               Text="Drop files here"
                               Foreground="#44FFFFFF"
                               FontSize="11"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               IsHitTestVisible="False"/>

                    <Thumb Width="5"
                           HorizontalAlignment="Right"
                           Background="Transparent"
                           Cursor="SizeWE"
                           DragDelta="RightThumb_DragDelta">
                        <Thumb.Template>
                            <ControlTemplate TargetType="Thumb">
                                <Border Background="Transparent"/>
                            </ControlTemplate>
                        </Thumb.Template>
                    </Thumb>
                </Grid>

                <!-- ═══ RESIZE STRIP ═══ -->
                <Grid x:Name="ResizeStrip" Grid.Row="2">
                    <Thumb Background="Transparent"
                           Cursor="SizeNS"
                           DragDelta="BottomThumb_DragDelta">
                        <Thumb.Template>
                            <ControlTemplate TargetType="Thumb">
                                <Border Background="Transparent"/>
                            </ControlTemplate>
                        </Thumb.Template>
                    </Thumb>

                    <Thumb Width="16"
                           Height="8"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Bottom"
                           Cursor="SizeNWSE"
                           DragDelta="CornerThumb_DragDelta">
                        <Thumb.Template>
                            <ControlTemplate TargetType="Thumb">
                                <Canvas Width="16" Height="8">
                                    <Path Data="M 0,8 L 16,0 L 16,8 Z" Fill="#55FFFFFF"/>
                                </Canvas>
                            </ControlTemplate>
                        </Thumb.Template>
                    </Thumb>
                </Grid>

            </Grid>
        </Border>
    </Border>
</Window>
